name := "RETAS"

version := "1.1"

scalaVersion := "2.11.4"

mainClass in assembly := Some("edu.umass.ciir.RecursiveAlignmentTool")

// outputPath in assembly := { file( "./" + (jarName in assembly).value ) } 
